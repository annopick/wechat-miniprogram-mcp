- 所有 MCP 工具均遵循统一的 `ControlTool` 或 `AutoTool` 接口定义，包含 `name`、`description`、`inputSchema` 和 `handler` 四个标准字段。
- 控制类工具在处理项目路径参数时，普遍采用 `resolveProject` 辅助函数，优先使用传入参数，其次回退到 `WECHAT_PROJECT_PATH` 环境变量。
- API 响应统一通过 `formatMcpContent` 函数转换为 MCP 标准格式，根据内容类型自动映射为 `text`、`json` 或 `image` 类型的返回对象。