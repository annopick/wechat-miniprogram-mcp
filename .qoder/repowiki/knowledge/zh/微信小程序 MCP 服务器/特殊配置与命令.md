- **环境要求**：必须设置 `WECHAT_DEVTOOLS_PORT` 环境变量以连接开发者工具；若使用自动化功能需配置 `WECHAT_DEVTOOLS_CLI_PATH` 和 `WECHAT_PROJECT_PATH`。
- **构建校验**：执行 `npm run build` 时会自动运行 `node scripts/check-shebang.js`，防止打包后的 CLI 文件出现重复 shebang 导致执行失败。